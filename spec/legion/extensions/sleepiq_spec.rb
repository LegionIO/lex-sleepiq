RSpec.describe Legion::Extensions::Sleepiq do
  it 'has a version number' do
    expect(Legion::Extensions::Sleepiq::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
